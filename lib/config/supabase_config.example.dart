// Copy this file to `supabase_config.dart` and fill in your project values.
// The real file is gitignored so your keys don't get committed.
//
// Where to find these:
//   Supabase dashboard → Project Settings → API
//     - Project URL      → supabaseUrl
//     - anon / publishable key → supabaseAnonKey
//
// The anon key is safe to ship in the client because Row Level Security
// policies on every table restrict reads/writes to `auth.uid() = user_id`.
const supabaseUrl = 'https://YOUR-PROJECT-REF.supabase.co';
const supabaseAnonKey = 'YOUR-ANON-OR-PUBLISHABLE-KEY';
